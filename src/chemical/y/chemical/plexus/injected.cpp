
#include "y/chemical/plexus/injected.hpp"



namespace Yttrium
{
    namespace Chemical
    {
#if 0
        Injected:: Injected(const Cluster &cl) :
        Quantized(),
        Summator(cl->species->size),
        root(cl),
        next(0),
        prev(0)
        {

        }

        Injected:: ~Injected() noexcept
        {
        }


        void Injected:: reset() noexcept
        {
            forEach( & XAdd::free );
        }
#endif
    }

}
